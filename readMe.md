in prod tab of jenkins
  go to prod-build of that repo 
    if there is an issue with production ready, then build the project ( Build with Parameters) save the build number
  go the prod-deploy of that repo
    in release build, put the build number from prod-build.
   
